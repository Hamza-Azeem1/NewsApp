import 'package:flutter/material.dart';

class EbooksScreen extends StatelessWidget {
  const EbooksScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('eBooks'));
  }
}
